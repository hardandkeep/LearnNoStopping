#digits = ['1','2','3','4','5','6','7','8','9']
#我在这里犯了类型错误，我接下里是想做数值运算，但是我在创建数值列表的时候，想当然的全部单引号，实际创建的是str类型

digits = [1,2,3,4,5,6,7,8,9]
#这才是正确的数值列表

min(digits)

max(digits)

sum(digits)

print(min(digits))

print(max(digits))

print(sum(digits))

