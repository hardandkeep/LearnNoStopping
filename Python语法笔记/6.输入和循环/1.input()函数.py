#函数input()会让程序暂停运行，等待用户输入一些文本。获取用户输入后，python将其储存在一个变量中
#记住：input会将输入解读为字符串，而不是其他形式
'''
message = input('input your name,i will repeat to you!')
print(message)

#使用int()来获取数值输入
number = int(input('what is your age?'))
print(number)
   #input返回的是字符串形势，所以需要将其改变为数值类型，以便于以后的使用

#求模运算符：%，它将两个数相除并返回余数
   #如果一个数可以被另一个数整除，余数就为0，因此求模运算符返回0，可以利用这一点来判断奇数偶数

print(4%3)
'''

